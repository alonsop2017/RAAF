# RAAF - Resume Assessment Automation Framework
# Python dependencies

# PDF Processing
pdfplumber>=0.10.0
pymupdf>=1.23.0

# OCR fallback for image-based PDFs (requires tesseract-ocr system package)
pytesseract>=0.3.10
Pillow>=10.0.0

# DOCX Processing
python-docx>=1.1.0

# RTF Processing (some legacy resumes arrive as .doc-extension RTF)
striprtf>=0.0.26

# Configuration
PyYAML>=6.0

# Data Processing
pandas>=2.0.0

# HTTP Requests (for PCR API)
requests>=2.31.0

# Claude API (for AI-powered assessments)
# Pinned to <2.0.0: an unpinned rebuild silently picked up 1.2.0, which
# dropped `temperature` from Messages.create() and broke every assessment.
anthropic>=1.2.0,<2.0.0

# Web Application
fastapi>=0.109.0,<0.116.0
uvicorn>=0.27.0
starlette>=0.37.0,<1.0.0
jinja2>=3.1.0,<3.1.5
python-multipart>=0.0.6

# Authentication
authlib>=1.3.0
httpx>=0.26.0
itsdangerous>=2.1.0
cryptography>=42.0.0
sqlalchemy>=2.0.0
bcrypt>=4.1.0

# Development/Testing
pytest>=7.4.0
black>=24.0.0
